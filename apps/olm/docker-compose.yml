services:
  olm:
    image: fosrl/olm:${OLM_VERSION:-1.2.0}
    container_name: olm
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      - PANGOLIN_ENDPOINT=${PANGOLIN_ENDPOINT}
      - OLM_ID=${OLM_ID}
      - OLM_SECRET=${OLM_SECRET}
